import { css } from '@emotion/react'
import { FC } from 'react'

export const Select: FC = () => {
  return (
    <div
      css={css({
        backgroundColor: 'red',
      })}
    >
      hello
    </div>
  )
}
